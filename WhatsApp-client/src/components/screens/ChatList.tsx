import React from "react";
import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-view";

const ChatList = () => {
  return (
    <View>
      <Text>ChatddList</Text>
    </View>
  );
};

export default ChatList;
